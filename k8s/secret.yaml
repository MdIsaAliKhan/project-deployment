# ─────────────────────────────────────────────────────────────────────────────
# IMPORTANT: Before applying, base64-encode your values:
#   echo -n "your_value" | base64
#
# Then replace the placeholder values below.
# Never commit real secrets to Git — use AWS Secrets Manager or Sealed Secrets.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: lms-secret
  namespace: lms
type: Opaque
data:
  # echo -n "your_db_password" | base64
  MYSQL_PASSWORD: Y2hhbmdlbWU=          # changeme  ← replace

  # echo -n "your_flask_secret_key" | base64
  SECRET_KEY: c3VwZXItc2VjcmV0LWtleQ==  # super-secret-key  ← replace
